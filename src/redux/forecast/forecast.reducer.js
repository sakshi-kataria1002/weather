//import { USER_ACTION_TYPES } from "./user.type"
const INTIAL_STATE = {
    searchCity:'',
    searchCountry:''
}

const forecastReducer = (state = INTIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default forecastReducer